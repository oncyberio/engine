vvUv = ((uv.xy) - 0.5);
// vvUv = uv;

// vec3 ssscale;
// ssscale.x = length(modelMatrix[0].xyz);
// // ssscale.y = length(modelMatrix[1].xyz);
// ssscale.z = length(modelMatrix[2].xyz);

// vvUv.xy /= vec2(ssscale.x, ssscale.z);