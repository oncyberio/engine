// float dist = distance( vWorldPosition.xyz, cameraPosition );

// float scan =  1.0 - smoothstep( 0.0, 5.0, abs(dist -  scanTimer  * 20.0 ) );

// gl_FragColor.rgb = mix( gl_FragColor.rgb,  gl_FragColor.rgb * 2.0, 0.7 * scan);
